# ============================================================
# AdamoPay Partner Manager — Dependencias
# ============================================================

# Framework web
streamlit>=1.32.0

# Base de datos
SQLAlchemy>=2.0.0          # ORM / query builder (compatible SQLite + PostgreSQL)
alembic>=1.13.0            # Migraciones de esquema

# Validación de datos
pydantic>=2.6.0
email-validator>=2.1.0

# Seguridad (hashing de contraseñas)
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0   # JWT tokens

# Persistencia de sesión (cookies)
extra-streamlit-components>=0.1.71

# Variables de entorno
python-dotenv>=1.0.0

# Utilidades de fechas
python-dateutil>=2.9.0

# Visualización (gráficas del dashboard)
plotly>=5.20.0

# Descargas HTTP (Centro Documental — Google Drive)
requests>=2.31.0

# Inteligencia Artificial (opcionales — activa el proveedor en .env)
google-generativeai>=0.7.0   # AI_PROVIDER=gemini
openai>=1.30.0               # AI_PROVIDER=openai
pandas>=2.2.0

# Exportación a Excel (reportes)
openpyxl>=3.1.0
xlsxwriter>=3.2.0

# Tests
pytest>=8.0.0
pytest-cov>=5.0.0

# PostgreSQL (producción — Railway)
psycopg2-binary>=2.9.9
